{
  "name": "battleships-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "auth:connect": "npm run build && node dist/src/auth-connect.js",
    "play": "npm run build && node dist/src/play.js",
    "analyze": "npm run build && node dist/src/analyze.js"
  },
  "dependencies": {
    "@auth/agent": "latest"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  }
}
